pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/umerBlueiris/nodejsbanking.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }

        stage('Build') {
            steps {
                // Add your custom build steps here if needed
                // For example, running tests or linting
            }
        }
    } // Closing brace for stages block
} // Closing brace for pipeline block

